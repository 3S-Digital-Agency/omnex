<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true">
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="DB_CONNECTION" value="pgsql"/>
        <env name="DB_DATABASE" value="omnex_test"/>
        <!-- Deterministic test environment: the sandbox OAuth provider must be
             enabled, and FRONTEND_URL/APP_URL must match the origin and RP ID
             used by the WebAuthn test kit (http://localhost:5173 / localhost)
             so attestations pass regardless of the developer's .env. -->
        <env name="APP_URL" value="http://localhost:8000"/>
        <env name="FRONTEND_URL" value="http://localhost:5173"/>
        <env name="SOCIAL_SANDBOX_ENABLED" value="true"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="OMNEX_STREAM_DRIVER" value="inprocess"/>
    </php>
</phpunit>
